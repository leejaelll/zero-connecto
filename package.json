{
  "private": true,
  "scripts": {
    "serve": "npx live-server public --host=localhost --port=3000 --no-browser",
    "compile": "",
    "watch": "sass sass:public/assets/styles --watch --update"
  }
}
